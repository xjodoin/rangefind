<!-- Status-bar icon for the navigation foreground service. Small icons are
     drawn as a mask, so this is one flat opaque shape and no colour: the
     chevron of the vehicle marker, which is what the notification is about. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M12,2.5 L20,20.5 L12,16.4 L4,20.5 Z" />
</vector>
