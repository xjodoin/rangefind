---
title: About
---

# About page distinct content xylophone marker

This About page carries a deliberately distinctive word — xylophone — so an
end-to-end search test can assert that querying for it returns exactly this
page and nothing else.

Placement demo: the `<rangefind-search>` custom element used directly in a
Markdown/MDX page. Once the plugin's script tag is injected site-wide, the
element upgrades on the client with no React wrapper required.

<rangefind-search src="/rangefind/" placeholder="Search the docs…"></rangefind-search>
