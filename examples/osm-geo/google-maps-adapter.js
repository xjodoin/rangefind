// Copy this file into an application or import these helpers directly from
// `rangefind/osm`. Keeping the example as a re-export prevents the migration
// guide from drifting away from the tested package implementation.
export {
  createRangefindMapsAdapter,
  toMigrationPlace
} from "rangefind/osm";

export { createRangefindMapsAdapter as default } from "rangefind/osm";
